# 医学装备不良事件：设计文档与 spd-sb 设备侧实现对齐说明

> 依据：`spd-sb/医学装备全生命周期管理系统不良事件功能解析.docx`（以下简称「设计文档」）  
> 对照范围：**spd-sb 前端（设备档案）** 及 **spd 后端 `equipment_info` 相关实现**（设备信息接口与持久化）  
> 更新说明：本文档用于记录「目标设计」与「当前代码」差异及兼容演进建议。

---

## 〇、不良事件子表建表与审计规范（追加要求）

以下为本期**数据模型强制约定**，已体现在 `equipment_adverse_event` 及后端实现中：

| 要求 | 落实说明 |
|------|----------|
| **主键 UUID7** | 子表主键 `id` 使用 `UUID7.generateUUID7Simple()`（36 位无连字符，与 `equipment_info.id` 风格一致）。 |
| **创建者 / 创建时间** | 字段 `create_by`、`create_time`；插入子表行时由 `SecurityUtils.getUserIdStr()` 与当前时间写入。 |
| **更新者 / 更新时间** | 字段 `update_by`、`update_time`；更新子表行时维护。 |
| **外键 varchar** | `equipment_id` 为 `varchar(32)`，逻辑关联 `equipment_info.id`（不设物理外键约束，与现有设备表风格一致）。 |
| **租户** | `tenant_id` 为 `varchar(36)`，与 `equipment_info.tenant_id` 及 `SecurityUtils.requiredScopedTenantIdForSql()` 一致，查询/更新均带租户条件。 |
| **逻辑删除** | 字段 `del_flag`：`0` 在册，`2` 已删除；并包含 **`delete_by`（删除者）**、**`delete_time`（删除时间）**；删除设备档案或从表单移除子行时，对子表做逻辑删除并写入删除者、删除时间。 |

**建表脚本位置**：`spd/spd-admin/src/main/resources/sql/mysql/equipment/table.sql`（`equipment_adverse_event` 段）。部署需在目标库执行该 DDL（或等价增量脚本）。

---

## 一、设计文档要点摘要

设计文档将不良事件定位为**全生命周期安全核心模块**，强调：

1. **闭环流程**：上报 → 评估 → 处理 → 跟踪 → 闭环验证 → 监管上报（及与运维工单联动）。
2. **合规**：参照《医疗器械不良事件监测和再评价管理办法》等，强调时限、可追溯、权限与审计。
3. **数据与集成**：UDI 扫码自动带出设备档案；移动端；物联网异常触发；**第三方不良事件系统**通过接口实现台账输出、事件回传与设备匹配。
4. **功能域**：事件管理、设备关联、协同处理、风险预警、统计分析、合规管理等。

---

## 二、当前 spd-sb「设备侧」已实现内容

### 2.1 界面位置与形态

在 **设备信息管理**（`src/views/equipment/equipmentInfo/index.vue`）中，设备档案表单包含 **「不良事件」** 页签（`el-tab-pane`，`name="adverseEvent"`），以**子表（多行）**形式维护 `form.adverseEventList`。

### 2.2 前端子表字段（当前）

| 列/字段 | 说明 |
|--------|------|
| `eventCode` | 事件编码（手工录入） |
| `archiveCode` | 当前设备档案编码（手工录入） |
| `eventDate` | 事件日期 |
| `reporter` | 报告人（用户下拉，存用户 id） |
| `eventType` | 事件类型：设备故障 / 操作失误 / 安全隐患 / 其他 |
| `eventLevel` | 事件等级：一般 / 严重 / **重大** |
| `eventDescription` | 事件描述 |
| `handlingMeasures` | 处理措施 |
| `handlingResult` | 处理结果 |
| `remark` | 备注 |

提交设备保存时，`submitForm` 将 **`this.form` 整体拷贝**为 `submitData` 调用 `addEquipment` / `updateEquipment`（`/equipment/info`）。

### 2.3 与设备运维相关能力（独立模块）

项目中存在 **设备维修**（`equipmentRepair` 等）等运维类页面与接口，**当前未发现**与 `adverseEventList` 的自动联动或状态同步代码（与文档「维修工单转不良事件」不一致，见下文差距表）。

---

## 三、代码与设计文档的差异与冲突点

### 3.1 持久化与数据模型（已落库）

| 项 | 说明 |
|------|------|
| **子表** | `equipment_adverse_event` 存储不良事件行；主键、审计、逻辑删除字段见上文「〇、建表与审计规范」。 |
| **实体与接口** | `EquipmentInfo` 增加 `adverseEventList`（`List<EquipmentAdverseEvent>`）；`EquipmentInfoServiceImpl` 在 **新增/修改** 设备后**同事务**同步子表；**按主键查详情 / 按资产编码查** 时回填 `adverseEventList`。 |
| **同步规则** | `adverseEventList == null` 时不改子表（兼容旧客户端不传该字段）；传 **空数组 `[]`** 会逻辑删除该设备下全部在册子行；有内容的行无 `id` 则插入，有 `id` 则更新；库中已有且未出现在本次提交中的 `id` 会被逻辑删除。 |
| **设备逻辑删除** | 设备走 `deleteEquipmentInfo*` 时，会先对该设备的不良事件子表批量逻辑删除（写入 `delete_by` / `delete_time`）。 |

**前端**：`loadEquipmentDetail` 合并接口返回后，若响应中含 `adverseEventList`（含各行 `id`），再次保存可正确更新/删除子行；**报告人**字段与后端 `Long reporter` 对齐，需为数值型用户 ID（与现有用户下拉一致）。

### 3.2 字段语义与命名（中优先级）

| 冲突 | 详情 |
|------|------|
| **`archiveCode` 复用** | 设备主表 `EquipmentInfo.archiveCode` 在业务/注释中多与 **「档案编号 / 保修类型」** 相关；不良事件子行亦使用 `archiveCode` 表示 **「当前设备档案编码」**。同一语义名在**主表与子表**中含义不同，易造成联调、导出、第三方对接时的混淆。 |
| **严重程度枚举** | 文档示例：**一般 / 严重 / 死亡**（及伤害、感染等类型维度）。前端为：**一般 / 严重 / 重大**。与国家报告表常用表述及文档不完全一致，统计与对外上报时需映射。 |
| **事件类型** | 文档列举：故障 / 伤害 / 感染等。前端为：设备故障 / 操作失误 / 安全隐患 / 其他。覆盖面不同，**「伤害」「感染」等未单列**，需用「其他」+ 描述或扩展字典兼容。 |

### 3.3 流程与能力缺口（相对设计文档）

下列能力在**当前设备档案不良事件子表**中**未见实现**（与文档差距，不等同于 bug）：

- 事件状态机（待上报 → 待评估 → 处理中 → …）  
- 时限提醒、督办、消息推送  
- 附件/照片/视频结构化上传（子表无附件列）  
- UDI 扫码在**设备不良事件**场景下自动填充（UDI 能力在 **高值耗材/订单** 等模块存在，**未挂到本页签**）  
- 监管系统直报、报告表自动生成  
- 第三方不良事件系统 **API 对接**（台账推送、事件回传匹配）  

---

## 四、兼容与演进方案（建议写入实施路线）

以下方案按**改造成本从低到高**排列，可并行组合。

### 4.1 短期兼容（保留现有前端结构）

1. **持久化落地**  
   - **已实施：方案 B** — 子表 `equipment_adverse_event`，在 `insertEquipmentInfo` / `updateEquipmentInfo` 上 **`@Transactional`** 内同步 `adverseEventList`；建表与字段规范见「〇」。  
   - **方案 A**（JSON 列）未采用；若后续需单表备份/导出可考虑双写，当前非必需。  

2. **枚举对齐（兼容旧数据）**  
   - 保留前端展示「重大」，后台或字典增加 **映射**：`重大` → 对外/监管口径的 `死亡或危及生命`（具体以院内上报字典为准）。  
   - 新增事件类型字典项（伤害、感染等），旧数据「其他」仍合法。  

3. **`archiveCode` 命名**  
   - 子表字段在接口层重命名为 `equipmentArchiveCode` / `assetArchiveCode`（后端新字段），前端逐步迁移；过渡期 **双写**：读时优先新字段，空则回落旧 `archiveCode`。  

### 4.2 中期：与第三方不良事件系统并存（对齐文档第七节）

与设计文档一致，推荐 **「临床仍在原不良事件系统上报 + 本系统做设备主数据与关联」**：

| 步骤 | 说明 |
|------|------|
| 台账输出 | 由本系统提供设备台账接口（UDI、序列号、院内编码、科室、状态等），供第三方下拉/扫码。 |
| 事件回传 | 定时或实时拉取第三方事件列表，按 **UDI / 序列号 / 资产编码** 与 `equipment_info` 匹配，写入关联表或回写扩展字段。 |
| 本系统展示 | 设备详情「不良事件」页签数据来源切换为：**本地录入子表 ∪ 第三方回传**，以 `sourceSystem`、`externalEventId` 区分。 |

此路径下，**监管上报仍走原系统**，满足文档「不改变原有监管上报路径」。

### 4.3 中长期：与设计文档闭环一致

- 独立「不良事件」业务模块（状态、权限、附件、时限引擎）。  
- 与 **维修工单** 建立按钮级或规则级联动（从维修单生成草稿事件、处理结果回写）。  
- UDI：在设备档案或不良事件录入处复用已有 UDI 解析能力，映射到设备主键。  

---

## 五、对开发与测试的明确提示

1. **库表**：上线前务必在目标 MySQL 执行 `equipment_adverse_event` 建表（见 `equipment/table.sql`），否则保存设备会失败。  
2. **回归**：保存设备 → 刷新详情 → 再打开「不良事件」页签，确认行数据与 `id` 回填；清空子表保存后，库中应为 `del_flag='2'` 且带删除者、删除时间。  
3. **列表接口**：`selectEquipmentInfoList` 仍为性能考虑**不附带** `adverseEventList`；列表页若需展示事件条数，需另加聚合接口或懒加载。  

---

## 六、参考代码位置（便于评审）

| 说明 | 路径 |
|------|------|
| 不良事件页签与子表 | `spd-sb/src/views/equipment/equipmentInfo/index.vue`（`adverseEvent` / `adverseEventList`） |
| 设备信息 REST | `spd/spd-biz/src/main/java/com/spd/equipment/controller/EquipmentInfoController.java` |
| 设备信息实体与表映射 | `spd/spd-biz/src/main/java/com/spd/equipment/domain/EquipmentInfo.java`、`mapper/equipment/EquipmentInfoMapper.xml` |
| 不良事件子表实体 / Mapper / SQL | `EquipmentAdverseEvent.java`、`EquipmentAdverseEventMapper.java/xml`、`sql/mysql/equipment/table.sql` |
| 子表同步逻辑 | `EquipmentInfoServiceImpl`（`syncAdverseEventsAfterSave`、`fillAdverseEventList`） |

---

## 七、小结

- **设计文档**描述的是**企业级、合规闭环、可对接监管与第三方**的不良事件体系。  
- **当前**：设备档案已具备 **PC 端子表 + 子表落库**（`equipment_adverse_event`，UUID7 主键、审计字段、varchar 设备外键、逻辑删除含删除者/删除时间），与文档在**流程、监管上报、第三方对接**等方面仍有差距。  
- 后续可通过 **枚举/字段映射、第三方回传、独立模块** 等逐步逼近文档目标。

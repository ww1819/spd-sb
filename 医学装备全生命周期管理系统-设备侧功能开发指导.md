# 医学装备全生命周期管理系统 — 设备侧功能开发指导

> **依据文档**：`spd-sb/医学装备全生命周期管理系统功能介绍_完整版.docx`（以下简称《功能介绍》）  
> **对照代码**：`spd-sb` 前端（设备管理菜单及 `src/views/equipment/**`）、`spd` 后端设备域接口（`/equipment/**` 等）  
> **文档定位**：作为 **设备系统（spd-sb + spd 设备域）** 的需求说明、差距分析、数据贯通原则与 **迭代开发优先级** 的单一事实来源（SSOT），供产品、后端、前端与实施统一引用。

---

## 1. 目标与范围

### 1.1 业务目标

《功能介绍》从三条主线描述医学装备管理：

| 主线 | 文档侧重 | 对设备系统的价值 |
|------|-----------|------------------|
| **报修维修** | 多端报修、扫码签到、临床评价、统计分析 | 服务闭环、响应可计量、与设备台账绑定 |
| **质量控制（巡检 + 保养）** | 标准库、计划、执行、异常闭环、档案与统计 | 预防性维护、合规留痕、与设备一机一档 |
| **不良事件** | 上报—评估—处理—跟踪—闭环—监管、第三方对接 | 安全合规、设备档案联动、避免重复录入 |

本指导书将上述能力与 **当前 spd-sb 已实现菜单/页面** 对齐，并明确 **扩展与补全** 方向，避免 **死链（无入口/无接口）** 与 **数据孤岛（业务表之间无稳定外键与回填）**。

### 1.2 系统边界（设备侧）

- **包含**：设备主数据与档案、入库/领用/借用/归还/调拨/分配/报废、维修与保养（文档中的 PM 与现场维修）、巡检（文档中的日常巡查）、采购申请与审批入口、盘点、设备分类与基础主数据、不良事件与设备档案的关联（已部分落地）。  
- **不包含（可在接口层对接）**：国家不良事件监测直报、完整 APP 离线能力、物联网实时采集（可作为二期集成）。  
- **与耗材侧关系**：`spd-sb` 路由已收敛为设备管理为主；若需「保养更换耗材」与耗材库存联动，通过 **物料编码 / 申领单接口** 对接，不在本仓库硬编码耗材 UI。

---

## 2. 《功能介绍》与设备侧模块映射

### 2.1 文档结构索引

- **第一部分**：报修维修（统一入口、扫码签到、评价、统计）。  
- **第二部分**：质量控制 — 巡检管理、保养管理、通用预警/审计/大屏/配置。  
- **第三部分**：不良事件（闭环流程、模块划分、技术要点、第三方对接）。

### 2.2 spd-sb 菜单与源码路径（当前实现）

以下与 `src/router/index.js` 中 `/equipment` 子路由一致，便于 **菜单—路由—页面—API** 不断链：

| 菜单名称 | 路由 path | 前端页面 |
|----------|-----------|----------|
| 设备信息 | `equipment/equipmentInfo` | `views/equipment/equipmentInfo/index.vue` |
| 设备分类 | `equipment/equipmentCategory` | `views/equipment/equipmentCategory/index.vue` |
| 设备档案 | `equipment/equipmentFile` | `views/equipment/equipmentFile/index.vue` |
| 设备巡检 | `equipment/equipmentInspect` | `views/equipment/equipmentInspect/index.vue` |
| 设备维修 | `equipment/equipmentRepair` | `views/equipment/equipmentRepair/index.vue` |
| 设备保养 | `equipment/equipmentMaintain` | `views/equipment/equipmentMaintain/index.vue` |
| 设备借用 / 归还 / 领用 | `equipmentBorrow` / `equipmentReturn` / `equipmentReceive` | 对应 `equipmentBorrow` 等目录 |
| 设备入库 | `equipmentStorage` | `views/equipment/equipmentStorage/index.vue` |
| 设备报废 | `equipmentScrap` | `views/equipment/equipmentScrap/index.vue` |
| 设备调拨 / 分配 | `equipmentTransfer` / `equipmentAllocate` | 对应目录 |
| 设备盘点 | `equipmentCheck` | `views/equipment/equipmentCheck/index.vue` |
| 设备采购申请 / 采购审批 | `purchaseApplication` / `purchaseApproval` | `purchaseApplication`、`purchaseApproval` 目录 |

**仓库内存在但未挂在上述路由的页面**（易形成「代码有、菜单无」的死链，开发指导建议二选一）：

| 路径 | 说明与建议 |
|------|------------|
| `views/equipment/assetInventory/index.vue`、`assetLedger/index.vue` | 资产盘点单、资产台账类能力；若产品需要，应 **增加路由与菜单** 或与「设备盘点 / 设备信息」合并入口并文档化。 |
| `views/equipment/equipmentInspection/index.vue`、`equipmentUse/index.vue`、`measuringCategory`、`supplier`、`manufacturer`、`brand` 等 | 若仍属设备域范围，应纳入 **路由/动态菜单** 或在本文档「暂缓/下线」表中登记，避免维护盲区。 |

### 2.3 典型后端 API 前缀（与 spd-sb 中 `src/api/equipment/*.js` 对应）

| 业务 | 前缀示例 | 说明 |
|------|-----------|------|
| 设备主档 | `/equipment/info` | 列表、详情、增删改、按资产编码查询 |
| 维修 / 保养 / 巡检 等 | `/equipment/repair`、`/equipment/maintain`、`/equipment/inspect` 等 | 与各自 `api/equipment/*.js` 一致 |
| 入库 / 报废 / 调拨 等 | `/equipment/storage`、`equipmentScrap` 等 | 以实际 Controller 为准 |

**防孤岛原则**：凡在设备详情或报表中要展示的子业务（维修单、巡检记录、保养记录、不良事件行），**详情接口应能携带或通过 equipmentId/assetCode 一键拉取**，列表页若性能原因不展开，需 **单独提供子资源分页接口** 并在前端设备详情中懒加载。

---

## 3. 分主题功能说明（整合文档 + 现状 + 扩展）

### 3.1 报修维修（文档第一部分）

**文档要点**：Web / APP 扫码 / 无账号 / 电话归集；工程师现场扫码签到与耗时；维修后临床评价；报修效率、满意度、健康度分析。

**当前代码侧**：

- 已有 **设备维修** 页面与接口（`equipmentRepair`），具备工单类管理基础。  
- **未见**与《功能介绍》一一对应的：无账号报修、电话坐席录入、工程师扫码签到、临床满意度与维修单强绑定、全院级维修分析大屏等（可作为迭代项）。

**扩展与防孤岛**：

1. **主数据关联**：报修/维修单必须持久化 **`equipment_id`（varchar）** 或 **`asset_code`（院内资产编码）** 二者至少其一，且与 `equipment_info` 可解析关联；禁止仅依赖设备名称。  
2. **与不良事件**：文档要求「维修工单可转不良事件」— 建议维修单增加 **「转不良事件」** 动作，生成草稿行并 **预填 `equipment_id`**（见 3.3）。  
3. **与巡检**：文档「维修完成后自动触发专项巡检」— 建议以 **事件（维修完成）** 调用巡检计划接口或写入待办任务表，**关联维修单号**。

### 3.2 质量控制 — 巡检（文档第二部分 · 巡检）

**文档要点**：巡检标准库、计划、执行（含 UDI 扫码、照片、离线、电子签名）、异常分级与工单、一机一档、统计与趋势。

**当前代码侧**：

- 已有 **设备巡检**（`equipmentInspect`），可支撑「计划—执行—记录」的基础形态。  
- **标准库 / 版本 / 计划引擎 / 离线 / 科室确认签名** 等需对照后端表结构逐项核对（若仅有单表记录，则与文档「模块 1.1–1.5」存在差距）。

**扩展与防孤岛**：

1. 巡检记录表须 **`equipment_id` + `tenant_id`**，可选 **`plan_id`、`standard_version`**，避免无法追溯到当时采用的判定标准。  
2. 设备详情页应提供 **「该设备巡检时间轴」** 入口（跳转带 `equipmentId` 查询条件），避免只在全局列表可查。

### 3.3 质量控制 — 保养（文档第二部分 · 保养）

**文档要点**：保养标准分级、计划、执行（停用、报告、耗材更换关联耗材模块）、复检与报废评估触发。

**当前代码侧**：

- 已有 **设备保养**（`equipmentMaintain`）。  
- 「关联耗材管理模块」在 **纯设备前端** 中需通过 **API / 物料主数据** 实现，不宜复制耗材 UI。

**扩展与防孤岛**：

1. 保养更换耗材：子表存 **`material_code`、数量、批次**，与耗材系统通过编码对齐；设备侧不强制存耗材主表。  
2. 保养不合格触发维修：与 3.1 共用 **设备状态** 或 **工单状态机** 字段，避免双写冲突。

### 3.4 不良事件（文档第三部分）

**文档要点**：全流程状态、附件、UDI、监管上报、统计、与运维联动、第三方系统台账与回传。

**当前代码侧（已落地部分）**：

- **设备信息**页签 **「不良事件」** 子表，前端字段与 `adverseEventList` 提交至 `/equipment/info` 保存。  
- 后端 **`equipment_adverse_event` 子表** 与 `EquipmentInfoServiceImpl` 同事务同步；**`del_flag`：0 未删除，1 已删除**；逻辑删除写 **`delete_by`、`delete_time`**；查询详情/按资产编码查询时回填列表。

**扩展与防孤岛**：

1. **状态与附件**：当前子表以「描述类字段」为主；若对齐监管流程，可增加 **`status`、`source`（本系统/第三方）、`external_event_id`、附件 JSON 或附件子表**，且均带 **`tenant_id`**。  
2. **UDI**：设备主档若补充 UDI 字段，不良事件录入应支持 **扫码写 `equipment_id`**，避免重复录入。  
3. **第三方对接**：台账输出与事件回传与《功能介绍》第七节一致；回传后写入同一子表或扩展表，**设备详情统一展示**。

### 3.5 设备主数据与全生命周期其它环节

**设备信息**（`equipmentInfo`）作为 **一机一档核心**：采购/验收/入库、使用科室、财务信息、附属资料 JSON、**不良事件子表** 等应在产品层定义为 **唯一主档入口** 或与其 ID 强同步。

**建议关联键（防孤岛）**：

| 子业务 | 建议外键 / 关联键 | 说明 |
|--------|-------------------|------|
| 维修 / 保养 / 巡检 | `equipment_id` varchar | 与 `equipment_info.id` 一致 |
| 入库明细生成设备 | `asset_code` / 院内编码 | 入库转档时写回 `equipment_info` |
| 不良事件子行 | `equipment_id` + 可选 `archive_code` 冗余 | 冗余需与主档同步策略约定 |
| 跨模块报表 | `tenant_id` + 时间范围 | 所有统计接口必须带租户 |

---

## 4. 数据模型与新建表规范（强制）

后续设备域 **新建业务表**（含不良事件扩展表、巡检标准、保养复检等）须统一满足以下约定，并与现有 `equipment_info`、`equipment_adverse_event` 风格一致。

### 4.0 数据库规约（新增表强制）

设备域 **每新增一张业务表**，数据库层须同时满足：

| 类别 | 规约 |
|------|------|
| **主键** | 使用 **UUID7**，字段类型 **`varchar(36)`**（`id`）。 |
| **外键** | 一律为 **`varchar`** 类型（宽度与所引用主键一致，**建议 `varchar(36)`**，如 `equipment_id` 引用 `equipment_info.id`）。 |
| **创建审计** | **`create_by`（创建者）**、**`create_time`（创建时间）**。 |
| **更新审计** | **`update_by`（更新者）**、**`update_time`（更新时间）**。 |
| **逻辑删除** | 必须包含：**`del_flag`（删除标志，`1` 为已删除，`0` 为未删除）**、**`delete_by`（删除者）**、**`delete_time`（删除时间）**；业务删除仅允许走逻辑删除并写入删除者、删除时间，禁止默认物理删除（另有合规审批的除外）。 |
| **租户** | **`tenant_id`（租户 id）**，建议 **`varchar(36) NOT NULL`**；查询、更新、删除须带租户条件，与 `SecurityUtils.requiredScopedTenantIdForSql()` 及主表 `tenant_id` 语义一致。 |

> **说明**：主键 UUID7 在 Java 中可使用 `UUID7.generateUUID7Simple()`（32 位）或 `UUID7.generateUUID7()`（36 位）；列宽 **`varchar(36)`** 用于兼容两种形态。`del_flag` 与代码、Mapper 中 **`0` / `1`** 语义须严格一致，不得使用其它取值表示删除态。

### 4.1 主键与外键

| 项 | 规范 |
|----|------|
| **主键** | **`id` `varchar(36) NOT NULL`**，取值 **UUID7**（项目内 `com.spd.common.utils.uuid.UUID7`）；允许 **32 位无连字符** 或 **36 位带连字符**，字段宽度统一 **36** 以兼容两种形态。 |
| **外键** | 一律 **`varchar(36)`**（或与主键同宽），存储 **`equipment_info.id`** 或其它主表主键字符串；**不在库内强制物理外键** 时，须在文档与代码层保证写入前主表存在。 |
| **租户** | **`tenant_id` `varchar(36) NOT NULL`**，与 `equipment_info.tenant_id` 及 `SecurityUtils.requiredScopedTenantIdForSql()` 一致；**所有查询/更新/删除必须带租户条件**。 |

### 4.2 审计与逻辑删除

| 项 | 规范 |
|----|------|
| **创建** | `create_by`、`create_time` |
| **更新** | `update_by`、`update_time` |
| **逻辑删除** | `del_flag`：**`0` 未删除，`1` 已删除**；并必须包含 **`delete_by`（删除者）**、**`delete_time`（删除时间）**；删除动作不得物理抹除业务行（除非监管另有要求且单独审批）。 |

### 4.3 参考 DDL（不良事件子表，已纳入 `spd` 建表脚本）

表名：`equipment_adverse_event`。完整 DDL 见：

`spd/spd-admin/src/main/resources/sql/mysql/equipment/table.sql` 中 **`equipment_adverse_event`** 段。

**后端同步逻辑**见：`EquipmentInfoServiceImpl`（`syncAdverseEventsAfterSave`、`fillAdverseEventList`）。

---

## 5. 死链与数据孤岛 — 检查清单（迭代必查）

建议在发版前按表自检：

1. **路由/菜单**：`views/equipment` 下每个仍维护的 `index.vue` 是否都有 **静态或动态菜单入口**？  
2. **详情贯通**：从设备信息详情能否跳到 **维修 / 巡检 / 保养** 并自动带 **`equipmentId` 或 `assetCode`**？  
3. **保存贯通**：子业务保存是否 **回写或校验** `equipment_id`，避免孤儿单？  
4. **列表与详情**：列表接口不返回子表时，详情接口是否 **补全** `adverseEventList` 等（与现 `equipment/info` 行为一致）？  
5. **租户**：新接口是否全部走租户绑定，避免跨租户引用？  
6. **删除**：设备逻辑删除时，子表是否同步逻辑删除（含 **delete_by/delete_time**）？

---

## 6. 分期实施建议（指导优先级）

| 阶段 | 目标 | 说明 |
|------|------|------|
| **P0** | 主数据与台账不断链 | `equipment_info` 与入库/科室/资产编码一致；不良事件子表与详情回填稳定；菜单与路由一致。 |
| **P1** | 报修维修闭环增强 | 维修单与设备强绑定、可选评价、与巡检/不良事件联动字段。 |
| **P2** | 质控深度对齐文档 | 巡检/保养标准库与版本、计划引擎、异常工单、统计导出。 |
| **P3** | 监管与集成 | 第三方不良事件回传、国家模板导出、物联网与 APP 离线（按院方选型）。 |

---

## 7. 附录

### 7.1 关键前端路径

- 路由：`spd-sb/src/router/index.js`（`/equipment` 段）。  
- API：`spd-sb/src/api/equipment/*.js`。  
- 设备主表页：`spd-sb/src/views/equipment/equipmentInfo/index.vue`（含不良事件页签）。

### 7.2 关键后端路径（spd）

- 设备信息：`spd-biz` → `com.spd.equipment.controller.EquipmentInfoController`、`service.impl.EquipmentInfoServiceImpl`。  
- 不良事件子表：`EquipmentAdverseEvent`、`EquipmentAdverseEventMapper`。

### 7.3 与《功能介绍》Word 的关系

Word 为 **目标能力全集**；本 MD 为 **设备侧落地映射 + 差距 + 数据规范 + 迭代顺序**。需求变更时：**先改 Word/需求单，再同步更新本 MD 对应章节**，保持 SSOT。

---

**文档版本**：与仓库同步维护；建表字段以 `equipment/table.sql` 及实体类为准。

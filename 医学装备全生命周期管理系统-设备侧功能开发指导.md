# 医学装备全生命周期管理系统 — 设备侧功能开发指导

> **依据**：`spd-sb/医学装备全生命周期管理系统功能介绍_完整版.docx`（下称《功能介绍》）  
> **对照实现**：`spd-sb`（`src/views/equipment/**`、`src/router/index.js`）、`spd` 设备域（`com.spd.equipment`、`/equipment/**`）  
> **文档角色**：设备子系统需求、与 Word 的差距、**数据贯通与防孤岛**原则、**库表规约**、**SQL 与菜单脚本分工（table / column / menu）**、迭代优先级的 **单一事实来源（SSOT）**；产品 / 后端 / 前端 / 实施共用。

---

## 导读（建议阅读顺序）

| 角色 | 建议重点 |
|------|----------|
| **后端** | **§4** 新建表规约；**§4.2** 脚本与菜单落盘；**§3.6** 配件独立域；**§5** 发版自检。 |
| **前端** | **§2.2** 菜单—路由—页面；**§3** 各模块与详情联动；**§5** 路由死链。 |
| **产品 / 实施** | **§1** 边界；**§6** 分期；**§7.3** 与 Word 的同步方式。 |
| **库表 / DBA / 发版脚本** | **§4.2**（`table.sql` / `column.sql` / `menu.sql` 分工）；**§5** 第 8 条自检。 |

**术语（文中多次出现）**

- **死链**：页面或接口存在，但 **无菜单/无路由/无调用方**，用户或上下游无法触达。  
- **数据孤岛**：子业务单据 **缺少 `equipment_id` / `asset_code` / `tenant_id`** 等稳定关联，或列表与详情 **无法互相跳转与回填**。  
- **SPD 耗材栈**：低值/高值耗材域常用表与单据（如 `material`、`stk_io_bill`、`stk_io_bill_entry` 等），**与设备配件表禁止混用**（见 **§1.2、§3.6**）。

---

## 1. 目标与范围

### 1.1 业务主线（与《功能介绍》对齐）

| 主线 | Word 侧重 | 对设备系统的价值 |
|------|------------|------------------|
| **报修维修** | 多端报修、扫码签到、临床评价、统计分析 | 闭环可追溯、响应可计量、与台账绑定 |
| **质量控制（巡检 + 保养）** | 标准库、计划、执行、异常闭环、档案与统计 | 预防性维护、合规留痕、一机一档 |
| **不良事件** | 上报—评估—处理—跟踪—闭环—监管、第三方对接 | 安全合规、档案联动、减少重复录入 |

本指导书将上述能力与 **当前 spd-sb 已挂路由的能力** 对齐，并标出 **扩展方向** 与 **禁止项**（避免死链与孤岛）。

### 1.2 系统边界

**设备侧包含**

设备主数据与档案；整机入库 / 领用 / 借用 / 归还 / 调拨 / 分配 / 报废；维修、保养、巡检；采购申请与审批入口；盘点；分类与基础主数据；不良事件与主档关联（部分已落地）。

**默认不包含（可另立集成方案）**

国家不良事件监测直报；完整 APP 离线；物联网实时采集（可作为二期）。

**与 SPD 耗材域的边界（强制）**

- `spd-sb` 以 **设备管理** 为主菜单形态。  
- **设备配件**的档案、库存、入出库见 **§3.6**：须 **独立表 + 独立实体 + 独立接口前缀**，**禁止**与 SPD 耗材栈 **共用同一套业务表或同一套出入库单**（例如用 `stk_io_*`、科室耗材申领单承载配件出库）。  
- 院方若要求「医用耗材」与保养流程在行政上衔接：按 **独立集成项目** 设计接口与对账规则，**不纳入本 SSOT 的默认实现**；且 **禁止** 为省事把配件流水写入耗材出库单。

---

## 2. 《功能介绍》与设备侧模块映射

### 2.1 Word 结构索引

| 部分 | 内容 |
|------|------|
| 第一部分 | 报修维修 |
| 第二部分 | 质控：巡检、保养、通用预警/审计/大屏/配置 |
| 第三部分 | 不良事件全流程与第三方对接 |

### 2.2 菜单 · 路由 · 页面（与代码一致）

与 `spd-sb/src/router/index.js` 中父级 **`/equipment`** 子路由一致（完整路径形如 `/equipment/equipmentInfo`）：

| 菜单 | 路由 path（子级） | 前端页面 |
|------|-------------------|----------|
| 设备信息 | `equipmentInfo` | `views/equipment/equipmentInfo/index.vue` |
| 设备分类 | `equipmentCategory` | `views/equipment/equipmentCategory/index.vue` |
| 设备档案 | `equipmentFile` | `views/equipment/equipmentFile/index.vue` |
| 设备巡检 | `equipmentInspect` | `views/equipment/equipmentInspect/index.vue` |
| 设备维修 | `equipmentRepair` | `views/equipment/equipmentRepair/index.vue` |
| 设备保养 | `equipmentMaintain` | `views/equipment/equipmentMaintain/index.vue` |
| 设备借用 / 归还 / 领用 | `equipmentBorrow` / `equipmentReturn` / `equipmentReceive` | 对应同名目录下 `index.vue` |
| 设备入库 | `equipmentStorage` | `views/equipment/equipmentStorage/index.vue` |
| 设备报废 | `equipmentScrap` | `views/equipment/equipmentScrap/index.vue` |
| 设备调拨 / 分配 | `equipmentTransfer` / `equipmentAllocate` | 对应目录 |
| 设备盘点 | `equipmentCheck` | `views/equipment/equipmentCheck/index.vue` |
| 设备采购申请 / 采购审批 | `purchaseApplication` / `purchaseApproval` | `purchaseApplication`、`purchaseApproval` 目录 |

**未挂在上述路由的页面（易死链，须决策）**

| 路径 | 处理建议 |
|------|----------|
| `assetInventory`、`assetLedger` | 需要则 **补路由+菜单**，或与「设备盘点 / 设备信息」合并入口并 **在本 MD 更新说明**。 |
| `equipmentInspection`、`equipmentUse`、`measuringCategory`、`supplier`、`manufacturer`、`brand` 等 | 纳入路由/菜单，或在项目内 **登记暂缓或下线**，避免无人维护的孤儿页。 |

### 2.3 后端 API 与前端 `api` 对应关系

| 业务域 | URL 前缀示例 | 前端 |
|--------|----------------|------|
| 设备主档 | `/equipment/info` | `src/api/equipment/equipmentInfo.js` |
| 维修 / 保养 / 巡检 | `/equipment/repair` 等 | 同名 `api/equipment/*.js` |
| 入库 / 报废 / 调拨 等 | `/equipment/storage` 等 | 以实际 Controller 为准 |

**防孤岛（接口层）**

设备详情或报表需要的子数据（维修、巡检、保养、不良事件、将来配件流水）：**详情接口应返回或可一键按 `equipmentId`/`assetCode` 拉取**；列表接口若不展开子表，应提供 **子资源分页接口**，前端在详情页 **懒加载**。

---

## 3. 分主题说明（Word + 现状 + 扩展）

各小节统一三段：**文档要点** → **当前实现** → **扩展与约束**。

### 3.1 报修维修（Word 第一部分）

**文档要点**：Web / APP / 无账号 / 电话归集；工程师现场扫码签到；维修后临床评价；效率与满意度分析。

**当前实现**：`equipmentRepair` 工单能力具备基础；与 Word 全量能力（无账号报修、坐席录入、签到节点、评价绑定、分析大屏等）多为 **迭代项**。

**扩展与约束**

1. 维修单须持久化 **`equipment_id`（varchar）** 与/或 **`asset_code`**，且能解析到 `equipment_info`；**禁止**仅靠设备名称关联。  
2. 「转不良事件」：建议在维修单上提供动作，生成不良事件草稿并 **预填 `equipment_id`**（见 **§3.4**）。  
3. 「维修完成触发专项巡检」：建议用 **领域事件** 或待办表，**携带维修单号**，避免与巡检模块硬编码循环依赖。

### 3.2 巡检（Word 第二部分 · 巡检）

**文档要点**：标准库、计划、执行（扫码、照片、离线、签名）、异常分级、一机一档、统计。

**当前实现**：`equipmentInspect` 支撑「计划—执行—记录」的雏形；标准库/版本/计划引擎/离线/科室确认等与 Word「模块 1.1–1.5」可能仍有差距，需 **对照后端表结构** 评估。

**扩展与约束**

1. 巡检记录表必备 **`equipment_id`、`tenant_id`**；建议 **`plan_id`、`standard_version`** 以便追溯当时标准。  
2. 设备详情提供 **「本机巡检」** 入口（路由带 `equipmentId` 查询条件）。

### 3.3 保养（Word 第二部分 · 保养）

**文档要点**：保养分级与计划、执行（停用、报告、更换部件类表述）、复检与报废评估触发。

**当前实现**：`equipmentMaintain`；Word 中的「更换部件」在 **本仓库 SSOT** 中 **默认归为设备配件域**（**§3.6**），**不默认**接入 SPD 耗材主数据与耗材出入库表。

**扩展与约束**

1. **设备专用配件**（备件、易损件、厂家指定件）：通过 **`equipment_id` + 配件出入库 / 保养用料子表** 关联；**禁止**将配件领用记入 `stk_io_*` 等耗材单据。若品类属医用耗材且 **必须** 走科室耗材账：单独立项需求，**配件模型与耗材模型分离**（允许接口级对账，禁止表级混用）。  
2. 保养不合格 → 维修：与 **§3.1** 共用设备状态或工单状态机，**避免两处各写一套状态**。

### 3.4 不良事件（Word 第三部分）

**文档要点**：状态流、附件、UDI、监管上报、统计、与运维联动、第三方台账与回传。

**当前实现**

- 前端：`equipmentInfo` 页签「不良事件」、`adverseEventList` 随 `/equipment/info` 提交。  
- 后端：`equipment_adverse_event` 子表，`EquipmentInfoServiceImpl` 同事务同步；**`del_flag`：`0` 未删、`1` 已删**；逻辑删除写 **`delete_by`、`delete_time`**；按 id / 按资产编码详情 **回填列表**。

**扩展与约束**

1. 监管对齐时可增 **`status`、`source`、`external_event_id`**、附件子表等，均须带 **`tenant_id`**。  
2. 主档若补充 UDI，上报侧应支持 **扫码定位 `equipment_id`**。  
3. 第三方回传：与 Word 第七节一致；回传数据进入本域表后，**设备详情统一展示**。

### 3.5 设备主数据与关联键约定

`equipmentInfo` 为 **一机一档核心**；采购、验收、整机入库、科室、财务、附属资料、不良事件等，产品层应明确 **主档入口** 与 **主键 `id` 的权威来源**。

| 子业务 | 关联键 | 说明 |
|--------|--------|------|
| 维修 / 保养 / 巡检 | `equipment_id`（varchar） | 与 `equipment_info.id` 一致 |
| 整机入库 → 建账 | `asset_code` 等院内编码 | 转档写回 `equipment_info` |
| 不良事件子行 | `equipment_id` | 行内 `archive_code` 若冗余须有同步策略 |
| 配件出入库 | `accessory_id`、出入库主/子表主键 | **仅设备域表**；禁止与 `stk_io_*` 混单 |
| 报表 | `tenant_id` + 时间范围 | 统计接口 **必须** 带租户 |

### 3.6 设备配件出入库（独立域，禁止与耗材混表）

**原则**

配件（备件、易损件、专机部件等）的 **主数据、库存、入/出库、退库、盘点调整** 使用 **设备域独立表 + 独立实体**（建议包：`com.spd.equipment.domain`，类名如 `EquipmentAccessory*`，以评审为准）。**禁止**与 `material`、`stk_io_bill`、`stk_io_bill_entry` 等 **共用业务主表或同一套单号体系**。

**建议表形态**（字段须满足 **§4**）

| 对象 | 说明 |
|------|------|
| 配件主数据 | 编码、名称、规格、单位、适用设备类型/型号、安全库存等 |
| 配件库存 | 维度如 **仓/仓位 + 配件 id**；出入库只更新 **本域库存表** |
| 入/出库单 | **主表 + 明细**；明细含 `accessory_id`、数量、单价（可选）、**`source_bill_type` / `source_bill_id`**（维修单、保养单等） |
| 与整机 | 出库可带 **`equipment_id`** 或 **`repair_id` / `maintain_id`**；无物理外键时 **应用层校验** |

**接口与前端**

- Controller 前缀建议 **`/equipment/accessory/**`**；`spd-sb` **独立菜单**。  
- **禁止**在配件模块内用 **科室耗材出库接口** 冒充配件出库。

**与 Word 的表述差异**

Word 中「关联耗材管理模块」属行业常见叙述；**本仓库设备侧 SSOT** 采用 **配件与耗材数据模型分离**；跨域仅通过 **显式集成接口**，且不得破坏配件侧可追溯性。

---

## 4. 数据模型与新建表规约（强制）

以下适用于设备域 **所有新建业务表**（含不良事件扩展、巡检/保养标准与执行、**配件主数据及配件出入库** 等）。**配件类表禁止与耗材共用同一套出入库业务表。**

### 4.1 新增表字段清单（须同时满足）

| 类别 | 规约 |
|------|------|
| 主键 | **`id` `varchar(36) NOT NULL`**；取值 **UUID7，固定 36 位**（含连字符 `8-4-4-4-12`）；Java 使用 **`UUID7.generateUUID7()`**（`com.spd.common.utils.uuid.UUID7`），**不使用** `generateUUID7Simple()` 作为新建行主键。 |
| 外键 | 类型一律为 **`varchar`**；长度与所引用主键列一致，**建议 `varchar(36)`**（如 `equipment_id` 引用 `equipment_info.id`；若历史主键为 32 位无连字符，仍用 `varchar(36)` 列宽存储即可）。 |
| 创建审计 | `create_by`、`create_time` |
| 更新审计 | `update_by`、`update_time` |
| 逻辑删除 | **`del_flag`**：**`0` 未删除，`1` 已删除**；**必须**含 **`delete_by`、`delete_time`** |
| 租户 | **`tenant_id` `varchar(36) NOT NULL`**；**所有** 查询 / 更新 / 删除须带租户，与 `SecurityUtils.requiredScopedTenantIdForSql()` 及 `equipment_info.tenant_id` 一致 |

**实现说明**

- **`equipment_info` 等存量表** 若主键仍为 **32 位无连字符**，属历史兼容；**本指导书约束下新建表的主键** 一律 **36 位 UUID7**，与上表「主键」行一致。  
- **`del_flag` 仅使用 `0` / `1`**，与 `equipment_adverse_event` Mapper 一致。  
- 未建物理外键时，**写入前**须在 Service 校验主表行存在且租户一致。

### 4.2 脚本与菜单数据维护规约（强制）

设备域数据库与 **设备前端（sb）菜单/按钮权限** 变更时，须按仓库约定写入对应 SQL 文件（路径均在 `spd/spd-admin/src/main/resources/sql/mysql/equipment/` 下）：

| 变更类型 | 落盘文件 | 说明 |
|----------|----------|------|
| **新增整表**（`CREATE TABLE`） | **`table.sql`** | 与现有设备表同一风格；文末「覆盖说明」亦要求新表追加于此。 |
| **已有表新增字段**（`ALTER … ADD COLUMN`） | **`column.sql`** | 使用本文件内 **`add_table_column`** 存储过程调用，避免重复执行报错；**勿**用 `add_table_column` 拼整表。 |
| **新增菜单、路由页、接口权限、按钮**（`sb_menu` 及与 `sb_role_menu` 等授权关系） | **`menu.sql`** | 菜单 `perms` 须与后端 `@PreAuthorize` / `@ss.hasPermi` 及前端 `v-hasPermi` **字符串一致**；新增按钮后按需 **`INSERT IGNORE INTO sb_role_menu`** 或按父菜单批量授权（见现有 `menu.sql` 写法）。 |

**存量库说明**：若某环境 **从未执行** `table.sql` 中某段 `CREATE TABLE IF NOT EXISTS`，应执行该段或全量执行 `table.sql` 对应部分；`column.sql` 仅承载 **增量列**，不承载整表建表语句（整表 DDL 以 `table.sql` 为准）。

### 4.3 参考实现：不良事件子表

- **表名**：`equipment_adverse_event`  
- **DDL**：`spd/spd-admin/src/main/resources/sql/mysql/equipment/table.sql` 中对应段落（主键 **36 位 UUID7**）。  
- **增量说明**：`equipment/column.sql` 末尾有该子表与 `equipment_info` 关系的文字说明（无新增列时无需 `CALL`）。  
- **菜单**：设备信息菜单 `perms` 及 `equipment:info:*` 按钮见 **`equipment/menu.sql`**（与 `EquipmentInfoController` 对齐）。  
- **同步逻辑**：`EquipmentInfoServiceImpl`（`syncAdverseEventsAfterSave`、`fillAdverseEventList`）；**新建子行主键**使用 **`UUID7.generateUUID7()`**。

---

## 5. 迭代发版自检清单

1. **路由/菜单**：`views/equipment` 下在维护的页面是否均有入口。  
2. **详情贯通**：设备详情能否跳转维修/巡检/保养并携带 **`equipmentId` 或 `asset_code`**。  
3. **保存贯通**：子业务是否校验/回写 **`equipment_id`**，无孤儿单。  
4. **列表 vs 详情**：列表不展开子表时，详情是否可 **懒加载**子资源（与当前 `equipment/info` 行为一致）。  
5. **租户**：新接口是否全程租户隔离。  
6. **删除**：设备逻辑删除时，子业务是否同步逻辑删除（**`delete_by` / `delete_time`**）。  
7. **配件**：配件库存与流水是否 **仅设备域表**；是否存在 **`stk_io_*` / 耗材申领** 承载配件的临时实现（须整改）。  
8. **SQL / 菜单脚本**：新表是否已写入 **`equipment/table.sql`**；新字段是否已写入 **`equipment/column.sql`**（`add_table_column`）；新菜单/按钮/`perms` 是否与后端、前端一致并写入 **`equipment/menu.sql`**（见 **§4.2**）。

---

## 6. 分期实施建议

| 阶段 | 目标 | 说明 |
|------|------|------|
| **P0** | 主数据与导航不断链 | `equipment_info`、整机入库与资产编码、不良事件子表与详情回填、路由与菜单一致 |
| **P0+** | 配件独立账 | 配件主数据 + 库存 + 出入库主/子表（**§3.6、§4**）落地；与 SPD 耗材栈 **零表复用**（可与 P0 并行排期） |
| **P1** | 维修闭环增强 | 维修单强绑定设备、可选评价、与巡检/不良事件联动；领配件走 **配件出库** |
| **P2** | 质控对齐 Word | 巡检/保养标准库与版本、计划引擎、异常工单、统计导出 |
| **P3** | 监管与集成 | 第三方不良事件回传、国家模板导出、物联网与 APP 离线（按院方选型） |

---

## 7. 附录

### 7.1 关键路径（仓库内）

| 层级 | 路径 |
|------|------|
| 前端路由 | `spd-sb/src/router/index.js`（`/equipment`） |
| 前端 API | `spd-sb/src/api/equipment/*.js` |
| 设备主档页 | `spd-sb/src/views/equipment/equipmentInfo/index.vue` |
| 设备域建表 / 增量列 / 菜单 SQL | `spd/spd-admin/src/main/resources/sql/mysql/equipment/table.sql`、`column.sql`、`menu.sql`（维护规约见 **§4.2**） |

### 7.2 关键后端路径（spd）

| 模块 | 包 / 类 |
|------|---------|
| 设备信息 REST | `com.spd.equipment.controller.EquipmentInfoController` |
| 设备信息事务与子表 | `com.spd.equipment.service.impl.EquipmentInfoServiceImpl` |
| 不良事件子表 | `EquipmentAdverseEvent`、`EquipmentAdverseEventMapper` |
| 设备配件（规划） | 实体与 Mapper 放在 **`com.spd.equipment`** 下；**不要**放入 `com.spd.hc` 或仅服务耗材的 `warehouse` 聚合包（命名以评审为准） |

### 7.3 与《功能介绍》Word 的维护关系

- Word：**目标能力全集**。  
- 本 MD：**设备侧落地、差距、禁止项、库表规约、分期**。  
- 变更流程：需求或设计变更时，**先更新 Word/需求单，再同步本 MD 对应章节**；若涉及库表或菜单权限，**同步修改 `equipment/table.sql`、`column.sql`、`menu.sql`**（见 **§4.2**），保持 SSOT。

---

**版本说明**：与仓库同步；列名与长度以 **`equipment/table.sql`** 及实体为准；**新建表主键**以 **36 位 `UUID7.generateUUID7()`** 为准；**增量列、菜单权限**分别以 **`equipment/column.sql`**、**`equipment/menu.sql`** 为准（见 **§4.2**）。
